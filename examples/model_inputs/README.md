# Model Input Files

Place your real test point clouds here.

Recommended filenames for quick tests:

- `sofa.pcd`
- `input.ply`
- `airplane.pcd`

For Snowflake wrapper, prefer `.xyz` or `.txt` for easiest local run.
If you have `.pcd`/`.ply`, convert them to XYZ text before running model mode.
